{% extends 'app/base.twig' %}
{% block title %}
  Me contacter
{% endblock %}
{% block description %}>Vous avez un projet ou une question ? N'hésitez pas à me contacter. Je suis à votre disposition pour échanger et vous accompagner.{% endblock %}
{% block titlePage %}
  Me contacter
{% endblock %}


{% block content %}
  <h1 class="contact_h1">Me contacter</h1>
  <p class="contact_p">Vous avez un projet ou une question ? N'hésitez pas à me contacter. Je suis à votre disposition pour échanger et vous accompagner.</p>
  <div class="container contact">
    <div class=" bg_form">
      <h2>M'envoyer un message.</h2>

      {% for key,flashItem in app.flashes %}
        {% if key == "contact_form" %}
          <div class="alert alert-success">
            {{ flashItem[0] }}
          </div>
        {% endif %}
      {% endfor %}
      {{ form_start(form) }}
      <div class="row">
        <div class="col-12 col-lg-6">
          {{ form_row(form.name) }}
        </div>
        <div class="col-12 col-lg-6">
          {{ form_row(form.surname) }}
        </div>
        <div class="col-12">
          {{ form_row(form.email) }}
        </div>
        <div class="col-12">
          {{ form_row(form.message) }}
        </div>
        <captacha-form key="{{ HCAPTCHA_SITEKEY }}" field="contact_form"></captacha-form>

        <div class="mt-1 d-flex justify-content-center">
          <button class="btn btn-primary">Envoyée le message</button>
        </div>

        <p>
          En soumettant ce formulaire, j'accepte que les informations saisies soient utilisées pour me recontacter dans le cadre de ma demande.
          <a href="{{ path('app_legal_rgpd') }}" target="_blank">En savoir plus sur la gestion de vos données personnelles.</a>
        </p>


      </div>
      {{ form_end(form) }}
    </div>
  </div>
{% endblock %}
